# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
# Phase 10: bumped 0.0.20 -> 0.0.32 — a pip-audit scan found 6 known
# CVEs against 0.0.20 (fixed progressively through 0.0.22-0.0.31); this
# parser sits directly on untrusted upload bytes (every UploadFile in
# the codebase), the highest-value dependency to patch found by the
# audit. Full test suite re-run green after the bump — see
# docs/security/dependency-audit.md.
python-multipart==0.0.32

# --- Validation / Settings ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Redis ---
redis==5.2.1

# --- Auth / Security ---
# Phase 10: bumped 3.3.0 -> 3.5.0 — a pip-audit scan found known CVEs
# against 3.3.0 (algorithm-confusion / DoS class issues), fixed in
# 3.4.0+. This is the library that signs and verifies every access and
# refresh token in the system (app/core/security/tokens.py) — the
# single highest-value dependency in the whole audit to patch. `jose`'s
# public API (jwt.encode/jwt.decode) is unchanged across this range;
# full test suite re-run green after the bump — see
# docs/security/dependency-audit.md.
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Logging ---
structlog==24.4.0

# --- Cloud Storage (Phase 3) ---
google-cloud-storage==2.19.0
filetype==1.2.0

# --- Pub/Sub (Phase 8) ---
google-cloud-pubsub==2.39.1

# --- Image Processing (Phase 8) ---
Pillow==12.3.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
aiosqlite==0.20.0
